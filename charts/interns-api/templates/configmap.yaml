apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Release.Namespace }}
data:
  SERVER_PORT: "{{ .Values.config.serverPort }}"
  JPA_SHOW_SQL: "{{ .Values.config.jpaShowSql }}"
  JPA_DDL_AUTO: "{{ .Values.config.jpaDdlAuto }}"
